# Seoul Dimming Recommender - Frontend

## Startup Instructions
1. Navigate to directory: `cd frontend`
2. Install dependencies: `npm install`
3. Run dev server: `npm run dev`
4. Open browser at http://localhost:5173
